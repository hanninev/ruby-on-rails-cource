<p id="notice"><%= notice %></p>

<p>
  <h2><%= @brewery.name %></h2>
</p>

<p>
  <i>Established at <%= @brewery.year %></i>
</p>

<p>
  Number of beers <%= @brewery.beers.count %>
</p>

<p>
 <% @brewery.beers.each do |beer| %>
   <%= link_to beer.name, beer %>
 <% end %>
</p>

<p>
<% if @brewery.ratings.empty? %>
  brewery has not yet been rated!
<% else %>
  <%= %"Has #{@brewery.ratings.length} #{'rating'.pluralize(@brewery.ratings.length)}, avegage #{@brewery.average_rating}" %>
<% end %>
</p>

<%= link_to 'Edit', edit_brewery_path(@brewery) if current_user %> |
<%= link_to 'Back', breweries_path %>
